# nmssitfest_Team-_MSS
Team NMSS hackathon rep
this is the hackathojn
h

## New: Library Management (Admin)
A Library Management system has been added to the Admin Panel (tab: 📚 Library). It uses the existing color palette and provides endpoints under `/api/library` for book CRUD, borrow and return, and borrow records. See `frontend/README_LIBRARY.md` and `backend/README_LIBRARY.md` for details and manual testing steps.